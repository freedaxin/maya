var Lazy = require('lazy');
Lazy
	.range('[5..10]')
	.filter(function(item){
		return item >= 5;
	})
	.map(function(item){console.log("map: "+item);})
	.join(function(item){console.log("join: "+item);});
